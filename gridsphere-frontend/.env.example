# Base URL of the GridSphere Node API.
# In dev, leave this as /api - vite.config.ts proxies it to localhost:8000.
# In production, point it directly at your deployed API, e.g.
# VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=/api


